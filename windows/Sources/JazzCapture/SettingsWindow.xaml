<Window x:Class="JazzCapture.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Jazz Capture settings"
        Width="560"
        Height="620"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   x:Name="NoticeText"
                   Margin="0,0,0,12"
                   Padding="8"
                   Background="#FFF4E5"
                   TextWrapping="Wrap"
                   Visibility="Collapsed"
                   Text="" />

        <CheckBox Grid.Row="1"
                  x:Name="HighlightClicksBox"
                  Content="Highlight where I click on screen"
                  Click="OnDirty" />

        <TextBlock Grid.Row="2"
                   Margin="0,16,0,0"
                   FontSize="14"
                   FontWeight="SemiBold"
                   Text="Excluded apps (never captured)" />

        <TextBlock Grid.Row="3"
                   Margin="0,4,0,0"
                   Foreground="#555"
                   TextWrapping="Wrap"
                   Text="The whole desktop is captured during a session. Add apps here to exclude them - e.g. password managers, banking, personal apps. An entry matches anywhere inside an application's identity, ignoring case. Secure text fields are always masked everywhere." />

        <ListBox Grid.Row="4"
                 x:Name="ExcludedList"
                 Margin="0,8,0,0"
                 FontFamily="Consolas"
                 SelectionChanged="OnSelectionChanged" />

        <Grid Grid.Row="5" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <ComboBox Grid.Row="0"
                      Grid.Column="0"
                      x:Name="RunningAppsBox"
                      DisplayMemberPath="DisplayName"
                      SelectionChanged="OnSelectionChanged" />
            <Button Grid.Row="0"
                    Grid.Column="1"
                    x:Name="ExcludeRunningButton"
                    Content="Exclude a running app"
                    Width="160"
                    Margin="8,0,0,0"
                    IsEnabled="False"
                    Click="OnExcludeRunning" />
            <Button Grid.Row="0"
                    Grid.Column="2"
                    x:Name="RefreshButton"
                    Content="Refresh"
                    Width="80"
                    Margin="8,0,0,0"
                    Click="OnRefreshRunning" />

            <TextBox Grid.Row="1"
                     Grid.Column="0"
                     x:Name="ManualEntryBox"
                     Margin="0,8,0,0"
                     FontFamily="Consolas"
                     TextChanged="OnSelectionChanged" />
            <Button Grid.Row="1"
                    Grid.Column="1"
                    x:Name="AddManualButton"
                    Content="Add"
                    Width="160"
                    Margin="8,8,0,0"
                    IsEnabled="False"
                    Click="OnAddManual" />
            <Button Grid.Row="1"
                    Grid.Column="2"
                    x:Name="RemoveButton"
                    Content="Remove"
                    Width="80"
                    Margin="8,8,0,0"
                    IsEnabled="False"
                    Click="OnRemove" />
        </Grid>

        <StackPanel Grid.Row="6"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button x:Name="CloseButton"
                    Content="Close"
                    Width="110"
                    Margin="0,0,8,0"
                    IsCancel="True"
                    Click="OnClose" />
            <Button x:Name="SaveButton"
                    Content="Save"
                    Width="110"
                    IsDefault="True"
                    Click="OnSave" />
        </StackPanel>
    </Grid>
</Window>
