<Window x:Class="JazzCapture.LabelPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Label current task"
        Width="480"
        SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        Topmost="True"
        ResizeMode="NoResize"
        Loaded="OnLoaded">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   FontSize="14"
                   FontWeight="SemiBold"
                   TextWrapping="Wrap"
                   Text="What are you working on?" />

        <TextBlock Grid.Row="1"
                   Margin="0,6,0,0"
                   TextWrapping="Wrap"
                   Foreground="#555"
                   Text="Everything recorded from now until you end the label is bound to this task." />

        <TextBox Grid.Row="2"
                 x:Name="LabelBox"
                 Margin="0,12,0,0"
                 MaxLength="200"
                 TextChanged="OnTextChanged" />

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,12,0,0">
            <Button Content="Cancel"
                    Width="110"
                    Margin="0,0,8,0"
                    IsCancel="True"
                    Click="OnCancel" />
            <Button x:Name="StartButton"
                    Content="Start label"
                    Width="110"
                    IsDefault="True"
                    IsEnabled="False"
                    Click="OnStart" />
        </StackPanel>
    </Grid>
</Window>
