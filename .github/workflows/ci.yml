name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  # So a branch can be run against the full matrix before a pull request exists. Useful for the
  # jobs that produce artifacts rather than just a pass or a fail.
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  contract:
    name: Desktop capture contract
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: astral-sh/setup-uv@v8.2.0
      - run: uv run --script contract/validate_schemas.py
      - run: uv run --script contract/archive/validate_archives.py
      - run: uv run --script contract/live/validate_live_transport.py
      - run: uv run --script contract/live/validate_capture_coach_live.py
      - run: uv run --script contract/live/generate_capture_coach_fixtures.py --check
      - run: uv run --script contract/archive/container/generate_fixtures.py --check

  windows:
    name: Windows client
    runs-on: windows-latest
    timeout-minutes: 60
    defaults:
      run:
        working-directory: windows
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"
      - run: dotnet test --configuration Release
      # The tray host is Windows-only and has no unit tests: compiling it here is what keeps the
      # Win32 and UI Automation interop from rotting unnoticed between real-machine runs.
      - run: dotnet build --configuration Release Sources/JazzCapture/JazzCapture.csproj
      # And the installer for the same reason. A job that only compiles the app cannot tell broken
      # WiX authoring from working WiX authoring, and nobody notices until a release.
      - name: Build the per-user MSI
        shell: pwsh
        run: ./installer/build-msi.ps1
      # An MSI that builds is not an MSI that installs correctly, so the finished database is
      # opened and its tables asserted: per-user with no elevation, payload under
      # %LOCALAPPDATA%\Jazz\App, one HKCU Run value, and no uninstall rule that would take the
      # user's recordings with it.
      - name: Verify the MSI
        shell: pwsh
        run: ./installer/Verify-Msi.ps1
      - name: Test MSI qualification helpers
        shell: pwsh
        run: ./installer/tests/Test-MsiQualificationHelpers.ps1 -MsiPath ./installer/artifacts/Jazz.msi
      - name: Read installer version
        id: installer-version
        shell: pwsh
        run: |
          $version = dotnet msbuild installer/Jazz.Version.props -getProperty:JazzProductVersion -nologo
          if ($LASTEXITCODE -ne 0 -or [string]::IsNullOrWhiteSpace($version)) {
            throw "Could not read JazzProductVersion."
          }
          "version=$($version.Trim())" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
      # This runner starts with no Jazz profile. The harness independently proves that again before
      # it creates inert sentinels or calls msiexec; its explicit switch cannot override a dirty
      # profile. Process path and survival are CI evidence, not a claim that a tray icon was visible.
      - name: Qualify install, launch, repair and uninstall
        shell: pwsh
        run: |
          ./installer/tests/Invoke-MsiLifecycleQualification.ps1 `
            -MsiPath ./installer/artifacts/Jazz.msi `
            -ExpectedVersion '${{ steps.installer-version.outputs.version }}' `
            -EvidenceDirectory ./installer/artifacts/qualification `
            -AllowInstalledProductMutation
      # Defense in depth: this runs even when qualification failed. An earlier privacy failure
      # leaves a safe marker, so this step still fails and the artifact upload remains disabled.
      - name: Enforce qualification evidence privacy
        id: qualification_privacy
        if: always()
        shell: pwsh
        run: |
          Import-Module ./installer/tests/MsiQualification.psm1 -Force
          Assert-QualificationEvidencePrivacy `
            -EvidenceDirectory ./installer/artifacts/qualification
      - name: Upload privacy-safe MSI qualification evidence
        if: ${{ always() && steps.qualification_privacy.outcome == 'success' }}
        uses: actions/upload-artifact@v4
        with:
          name: jazz-capture-msi-qualification
          path: windows/installer/artifacts/qualification/
          if-no-files-found: warn
      - name: Upload the MSI
        uses: actions/upload-artifact@v4
        with:
          name: jazz-capture-msi-unsigned
          path: windows/installer/artifacts/Jazz.msi
          if-no-files-found: error

  windows-installer-crossbuild:
    # The optional path: the same per-user MSI, built where the client is actually developed. It is
    # gated here because an unverified build script is worse than none - it would rot into
    # producing something subtly unlike the package the Windows job ships. Nothing is uploaded from
    # this job; the artifact above is the one that ships.
    name: Cross-built MSI (wixl)
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"
      # Debian and Ubuntu ship msitools without wixl, so the cross build is exercised on macOS.
      - run: brew install msitools
      - run: ./windows/installer/build-msi.sh
      - run: ./windows/installer/verify-msi.sh

  windows-upgrade-matrix:
    name: Windows rollback-safe upgrade matrix
    runs-on: windows-latest
    timeout-minutes: 60
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"
      - name: Build isolated test-only MSI matrix
        shell: pwsh
        run: ./windows/installer/tests/Build-UpgradeTestMatrix.ps1
      - name: Verify exact test package identities and action sequences
        shell: pwsh
        run: ./windows/installer/tests/Verify-UpgradeTestMatrix.ps1
      - name: Run repair, replacement, rollback and uninstall matrix
        shell: pwsh
        run: |
          ./windows/installer/tests/Invoke-UpgradeTestMatrixQualification.ps1 `
            -EvidenceDirectory ./windows/installer/artifacts/test-only-upgrade/qualification `
            -AllowInstalledProductMutation
      - name: Enforce upgrade evidence privacy
        id: upgrade_privacy
        if: always()
        shell: pwsh
        run: |
          Import-Module ./windows/installer/tests/MsiQualification.psm1 -Force
          Assert-QualificationEvidencePrivacy `
            -EvidenceDirectory ./windows/installer/artifacts/test-only-upgrade/qualification
      - name: Upload privacy-safe upgrade evidence
        if: ${{ always() && steps.upgrade_privacy.outcome == 'success' }}
        uses: actions/upload-artifact@v4
        with:
          name: jazz-capture-msi-upgrade-qualification
          path: windows/installer/artifacts/test-only-upgrade/qualification/
          if-no-files-found: error
      - name: Prove release artifact selection excludes test-only packages
        shell: pwsh
        run: |
          $release = @(Get-ChildItem ./windows/installer/artifacts -File -Filter Jazz.msi)
          $testOnly = @(Get-Content ./windows/installer/artifacts/test-only-upgrade/generated/matrix.json -Raw | ConvertFrom-Json)
          if ($release.Count -ne 0) { throw 'Upgrade matrix job must not produce a release MSI.' }
          if ($testOnly.Count -ne 4) { throw 'Expected exactly four isolated test-only MSI outputs.' }

  macos:
    name: macOS client
    runs-on: macos-15
    defaults:
      run:
        working-directory: macos
    steps:
      - uses: actions/checkout@v6
      - name: Select latest Xcode
        run: |
          sudo xcode-select -s "$(ls -d /Applications/Xcode_*.app | sort -V | tail -1)"
          swift --version
      - run: swift build
      - run: swift test
